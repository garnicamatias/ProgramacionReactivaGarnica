import { FixedArrayPipe } from './fixed-array.pipe';

describe('FixedArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new FixedArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
