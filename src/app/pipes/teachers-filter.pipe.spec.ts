import { TeachersFilterPipe } from './teachers-filter.pipe';

describe('TeachersFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TeachersFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
