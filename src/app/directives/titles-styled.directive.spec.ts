import { TitlesStyledDirective } from './titles-styled.directive';

describe('TitlesStyledDirective', () => {
  it('should create an instance', () => {
    const directive = new TitlesStyledDirective();
    expect(directive).toBeTruthy();
  });
});
