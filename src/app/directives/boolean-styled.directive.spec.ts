import { BooleanStyledDirective } from './boolean-styled.directive';

describe('BooleanStyledDirective', () => {
  it('should create an instance', () => {
    const directive = new BooleanStyledDirective();
    expect(directive).toBeTruthy();
  });
});
